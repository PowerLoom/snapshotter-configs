{
  "config": [{
      "project_type": "baseSnapshot",
      "processor":{
        "module": "computes.pair_total_reserves",
        "class_name": "PairTotalReservesProcessor"
      },
      "preload_tasks": [
        "eth_price",
        "block_details"
      ]
    }
  ]
}
